apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "horilla-ph.serviceAccountName" . }}
  labels:
    {{- include "horilla-ph.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
